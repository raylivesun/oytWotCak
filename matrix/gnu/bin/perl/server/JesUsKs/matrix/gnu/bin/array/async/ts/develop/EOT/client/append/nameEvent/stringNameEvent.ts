/**
 * @param {string} nameEvent The name of the event
 * 
 */

function StringConstructorNameEvent(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    
    // TODO: these should be strings or numbers    

}

/**
 * @param {string} nameType The name of the type
 * 
 */

function StringConstructorNameEventType(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    
    // TODO: these should be strings or numbers    

}


/**
 * @param {string} nameSettings The name of the settings
 * 
 */

function StringConstructorNameSettings(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    
    // TODO: these should be strings or numbers    

}

/**
 * @param {string} nameSettings The name of the settings
 * 
 */

function StringConstructorSettings(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    
    // TODO: these should be strings or numbers    

}


/**
 * @param {string} nameSettings The name of the settings
 * 
 */

function StringConstructorLinearSettings(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    
    // TODO: these should be strings or numbers    

}

/**
 * Create a new element for the given element type and settings
 */

function StringConstEvent(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    
    // TODO: these should be strings or numbers    
    return StringConstructor.toString();
    
    // TODO: This should probably be a string constructor instead of a constructor
    if (StringConstructor.prototype != StringConstructor) {
        this.constructor = StringConstructor;
    } else if (StringConstructor.prototype) {
        this.constructor = StringConstructor;
    } else if (StringConstructor.prototype) {
        this.constructor = StringConstructor;
    } else if (StringConstructor.prototype) {
        this.constructor = StringConstructor;
    } else if (StringConstructor.prototype) {
        this.constructor = StringConstructor;
    } else if (StringConstructor.prototype) {
        this.constructor = StringConstructor;
    } else if (StringConstructor.prototype) {
        this.constructor = StringConstructor;
    } else if (StringConstructor.prototype) {
        this.constructor = StringConstructor;
    } else if (StringConstructor.prototype) {
        return this.constructor;
    }


    // TODO: This should probably be a string constructor instead of a constructor
    if (typeof StringConstructor.prototype.name === StringConstructor.prototype) {
    
        // StringConstructor.prototype.name
        KeyboardEvent = StringConstructor.prototype;
        KeyboardEvent = StringConstructor.prototype;
        KeyboardEvent.prototype = StringConstructor.prototype;
        KeyboardEvent.prototype.constructor = StringConstructor.prototype;
        KeyboardEvent.prototype = StringConstructor.prototype;
        KeyboardEvent.prototype = StringConstructor.prototype;
        
        // TODO: This should probably be a string constructor instead of a constructor
        prompt.caller.prototype = 0;
        prompt.toString.caller.arguments = "safescreen";
        prompt.prototype = StringConstructor.prototype;
        prompt.prototype.constructor = StringConstructor.prototype;

        // TODO: This should probably be a string constructor instead of a constructor
    }

    // TODO: This should probably be a string constructor instead of a constructor
    if (typeof StringConstructor.prototype.name === StringConstructor.prototype) {
    
        // StringConstructor.prototype.name
        KeyboardEvent = StringConstructor.prototype;
        KeyboardEvent = StringConstructor.prototype;
        KeyboardEvent.prototype = StringConstructor.prototype;
        KeyboardEvent.prototype.constructor = StringConstructor.prototype;
        KeyboardEvent.prototype = StringConstructor.prototype;
        KeyboardEvent.prototype = StringConstructor.prototype;
        
        // TODO: This should probably be a string constructor instead of a constructor
        prompt.caller.prototype = 0;
        prompt.toString.caller.arguments = "safescreen";
        prompt.prototype = StringConstructor.prototype;
        prompt.prototype.constructor = StringConstructor.prototype;

        // TODO: This should probably be a string constructor instead of a constructor
    }

    // TODO: This should probably be a string constructor instead of a constructor
    if (typeof StringConstructor.prototype.name === StringConstructor.prototype) {
    
        // StringConstructor.prototype.name
        KeyboardEvent = StringConstructor.prototype;
        KeyboardEvent = StringConstructor.prototype;
        KeyboardEvent.prototype = StringConstructor.prototype;
        KeyboardEvent.prototype.constructor = StringConstructor.prototype;
        KeyboardEvent.prototype = StringConstructor.prototype;
        KeyboardEvent.prototype = StringConstructor.prototype;
        
        // TODO: This should probably be a string constructor instead of a constructor
        prompt.caller.prototype = 0;
        prompt.toString.caller.arguments = "safescreen";
        prompt.prototype = StringConstructor.prototype;
        prompt.prototype.constructor = StringConstructor.prototype;

        // TODO: This should probably be a string constructor instead of a constructor
    }

    // TODO: This should probably be a string constructor instead of a constructor
    if (typeof StringConstructor.prototype.name === StringConstructor.prototype) {
    
        // StringConstructor.prototype.name
        KeyboardEvent = StringConstructor.prototype;
        KeyboardEvent = StringConstructor.prototype;
        KeyboardEvent.prototype = StringConstructor.prototype;
        KeyboardEvent.prototype.constructor = StringConstructor.prototype;
        KeyboardEvent.prototype = StringConstructor.prototype;
        KeyboardEvent.prototype = StringConstructor.prototype;
        
        // TODO: This should probably be a string constructor instead of a constructor
        prompt.caller.prototype = 0;
        prompt.toString.caller.arguments = "safescreen";
        prompt.prototype = StringConstructor.prototype;
        prompt.prototype.constructor = StringConstructor.prototype;

        // TODO: This should probably be a string constructor instead of a constructor
    }

    // TODO: This should probably be a string constructor instead of a constructor
 return function(){};       
}
