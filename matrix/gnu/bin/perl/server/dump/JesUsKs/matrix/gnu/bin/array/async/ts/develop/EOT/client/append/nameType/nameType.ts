/**
 * @param {string} nameType The name type of the object being created
 */

function StringConstructorNameType(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    

}


/**
 * @param {string} nameType The name type of the object being created
 */

function StringConstructorObjType(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    

}

/**
 * @param {string} nameType The name type of the object being created
 */

function StringConstructorType(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    

}

/**
 * @param {string} nameType The name type of the object being created
 */

function StringConstructorTypeName(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    

}

/**
 * @param {string} nameType The name type of the object being created
 */

function StringConstructorNameTypeName(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    

}

/**
 * @param {string} nameType The name type of the object being created
 */

function StringConstructorJavaType(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    

}


/**
 * @param {string} nameType The name type of the object being created
 */

function StringConstructorJavaTypeName(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    

}

/**
 * @param {string} nameType The name type of the object being created
 */

function StringConstructorJavaNameTypeName(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    

}

/**
 * @param {string} nameType The name type of the object being created
 */

function StringConstructorJavaTypeNameType(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    

}
