/**
 * @param {string} name The name of the element to be created
 */

function StringConstructorTopicRules(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    
    // TODO: these should be strings or numbers    

}



/**
 * Create a new element for the given element type and settings
 */

function StringObjEvent(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    
    // TODO: these should be strings or numbers    

}

/**
 * Create a new element for the given element type and settings
 */

function StringDocumentEvent(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    
    // TODO: these should be strings or numbers    

}


/**
 * Create a new element for the given element type and settings
 */

function StringWindowEvent(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    
    // TODO: these should be strings or numbers    

}

/**
 * Create a new element for the given element type and settings
 */

function StringElementEvent(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    
    // TODO: these should be strings or numbers    

}

/**
 * Create a new element for the given element type and settings
 */

function StringGeolocationEvent(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    
    // TODO: these should be strings or numbers    

}

/**
 * Create a new element for the given element type and settings
 */

function StringGeolocationElementEvent(Name: string, NameEvent: string, NameType: string, NameSettings: string) {
    
    // The name of the element to be created for the given
    Name = Name.toLowerCase();
    NameEvent = NameEvent.toLowerCase();
    NameType = NameType.toLowerCase();
    NameSettings = NameSettings.toLowerCase();
    
    // TODO: these should be strings or numbers    

}

